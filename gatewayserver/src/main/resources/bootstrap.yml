spring:
  application:
    name: gateway-server
  cloud:
    config:
      uri: http://configserver:8071
#      uri: http://localhost:8071

logging:
  level:
    com.netflix: WARN
    org.springframework.web: INFO
    com.optimagrowth: DEBUG